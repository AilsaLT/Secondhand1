<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ghl.wuhan.secondhand">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LogoActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".home_activity.search_activity">
        </activity>
        <activity android:name=".find_activity.sale">
        </activity>
        <activity android:name=".find_activity.buy">
        </activity>
        <activity android:name=".me_activity.user_login">
        </activity>
        <activity android:name=".me_activity.user_register">
        </activity>
        <activity android:name=".me_activity.reset_password_activity">
        </activity>
        <activity android:name=".sort_activity.sort_classify_activity">
        </activity>
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>